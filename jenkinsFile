/* Requires the Docker Pipeline plugin */
pipeline {
    agent { docker { image 'node:current-alpine3.20'
    
           label 'docker-on-vas'
    }
    
    }
    stages {
        stage('build') {
            steps {
                sh '''
                npm --version
                node --version
                '''
            }
        }
    }
}